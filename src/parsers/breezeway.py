"""Parse Breezeway CSV attachment into a list of task dicts."""

# TODO: Implement Breezeway parser
# Input: CSV string (decoded attachment content)
# Output: list of dicts with keys:
#   title, property, tags, due_date, status, estimated_time,
#   created_date, report_link, timezone
